export interface productInterface
{
    _id       ?: string;
    name      ?: string;
    price     ?: number;
    stock     ?: number;
    photo     ?: object;
    updatedAt ?: string[];

}//para manejo de tipos de datos en paràmetros de funciones